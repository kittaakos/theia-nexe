tasks:
  - init: >
      yarn &&
      yarn build &&
      yarn package &&
      ./dist/theia
github:
  prebuilds:
    master: true
    branches: true
    pullRequests: true
    pullRequestsFromForks: true
    addComment: false
    addBadge: false
